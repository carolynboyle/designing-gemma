# Post Notes: designing-gemma Dev.to Contest Submission
## Working title: "I Went to Learn Gemma 4 and Accidentally Paved Two Streets"

---

## Target

Dev.to "Weekend Challenge" contest — $500 build prize.
designing-gemma is the build: a structured experimental framework for
probing what Gemma 4 can actually do, documented as you go.

Deadline: May 24, 2026. 

---

## The Thesis

Forty years of programming means you can't walk past a problem without
building a door for it.

I sat down to learn Gemma 4. Before I could do that I needed a project.
Before I could build the project I needed a scaffolding tool. The tool
took an afternoon. It will save weeks across the life of the project.
Then, finally, Gemma.

This is not procrastination. This is engineering judgment. A newer
programmer would have done it by hand. You build the abstraction because
you know you'll need it again.

The interesting thing isn't "here's how to use Gemma 4." The interesting
thing is what happened on the way to using Gemma 4, and what it says
about how programming experience changes your relationship to problems.

---

## The Anti-Framing

This post is NOT "AI CHANGES EVERYTHING."
Dev.to is drowning in breathless newcomer amazement posts. This isn't one.

The framing: I'm not a newcomer being amazed. I'm an experienced
programmer treating this as what it actually is — a tool with a learning
curve. A new system is just a new dictionary. I used an AI to get the
dictionary. That's it.

Except the dictionary metaphor undersells it. It's more like: I went to
learn the neighborhood and on the way I paved two streets that needed
paving, because obviously.

---

## Structural Arc (Article Outline)

1. **I sat down to learn Gemma 4.**
   Needed a project. Looked at the repo structure I needed to create.
   Did a thirty-second cost-benefit analysis.

2. **First I built treekit.**
   "What kind of idiot would do something like this manually if they
   didn't have to?" Reads a markdown spec, creates folders, stub files,
   .gitkeep for empty folders. Took an afternoon. The spec IS the
   documentation — change it, rerun, structure updates. Treekit is now
   in dev-utils.

3. **Then I built the experiment framework.**
   designing-gemma: structured experiment runner, YAML configs, Jinja2
   prompt templates, Ollama client, run logs. The whole thing.

4. **Then I needed repo context for the prompts.**
   Gemma can't read the filesystem. Hallucinated beautifully. Built
   skeleton_reader (ast extraction) and repo_reader (prompt injection).
   skeleton_reader → eventually skeletonkit in dev-utils.
   repo_reader → eventually part of filekit in dev-utils.

5. **Also noted venvkit for later.**
   Config-driven venv management. Not today. But noted.

6. **Then I forgot my own rules.**
   Broke config_rules — hardcoded OS-specific Ollama host. Spent a
   morning formalizing config_rules.md instead of fixing it in five
   minutes. Now it's a reusable standard for all projects.

7. **Then, finally, Gemma.**
   The fear of pressing 1 on the menu after everything finally works.
   Watching tokens stream in real time. The hallucinations. The results.

8. **Gemma generated tools I'll actually keep.**
   Three new library tools emerged as side effects of building the
   framework. The project that was supposed to test a tool ended up
   producing tools.

9. **Experiment 09 closes the loop.**
   CRUD webui for experiment definitions, backed by postgres. Generated
   by Gemma. The tool built to learn Gemma ends up with a UI generated
   by Gemma. The student builds the classroom.

10. **Integration with Curator.**
    The postgres projects tracking database already exists. The webui
    fits into the existing Curator infrastructure. Experiment definitions
    become queryable, filterable, auditable — not just YAML files.
    designing-gemma isn't throwaway contest code. It's the beginning of
    a reusable evaluation harness.

---

## Specific Lines / Moments to Include

- "What kind of idiot would do something like this manually if they
  didn't have to?" — treekit origin

- "I went to learn the neighborhood and on the way I paved two streets
  that needed paving, because obviously." — thesis

- The fear of pressing 1 on the menu after everything finally works.
  The moment when all the infrastructure is running and suddenly you're
  not sure you want to find out if the actual thing works. Very relatable.
  Write this down before the feeling fades.

- Watching tokens stream in real time in the terminal. Need a screen
  recorder (OBS Studio). Worth capturing for the post.

- The Vetinari endpoint quote (generated by Gemma during experiments):
  "The endpoint will accept your request. Whether it chooses to respond
  promptly is, of course, entirely at its discretion."

- "The towel is mentioned twice in the experiment configs with zero
  explanation, which is exactly right."

- e2b vs e4b hallucination contrast: e2b honestly said "I have no
  filesystem context." e4b confidently invented an entire repo.
  Both are interesting data points. Neither is wrong, exactly.

- The Gregor Samsa observation: waking up one morning to find you've
  become a prompt engineer when you were perfectly happy being a systems
  programmer. Not far from the source material.

- "Acceptance of the unacceptable as though it were a minor
  inconvenience" — good description of current American social media
  and journalism. Explains why "kafkaesque" comes to mind so often.
  This belongs somewhere in the post.

- The division of labor between Claude (mechanical prompts) and Gemini
  (creative/voice prompts) should be documented explicitly. It's a
  genuine insight about multi-LLM workflows.

- The Dev.to contest is a way for Google to get unpaid software
  debugging done. Worth acknowledging this openly — it doesn't make
  the work less valuable, but clear eyes about what's happening is
  on-brand for this post.

---

## Section Heading (keep this one)

**"Interesting Things I Found Because I Forgot Things"**

Covers: Kafka training data finding, e2b vs e4b hallucination contrast,
empty prompt files producing instant completions, anything else that
fell out of the gaps between what was planned and what actually ran.

---

## Experiment 02 (Linter Cleanup) — Key Findings

This experiment was supposed to be the easy one. Fix trailing whitespace
and missing docstrings. It produced the most interesting findings of
the entire project.

### The failure mode taxonomy

Five distinct failure modes observed across 81 files × 2 models:

1. **Bail** (eval_count = 0) — e2b emits immediate EOS, returns nothing.
   Clean signal, easy to detect programmatically.
2. **Loop** — e2b repeats the same function stub endlessly until token
   limit. Deterministic, catchable with repetition detection.
3. **Truncation** — e2b runs out of token budget mid-correct-output.
   Was producing the right thing, just ran out of room.
4. **Feature extraction** — both models identify the most semantically
   distinctive section of a file, reproduce only that, discard the rest.
5. **Correct** — both models on finished, coherent, single-purpose files.

### Feature extraction blindness

Fletcher is 607 lines of finished, daily-use code. It reads a
Dr. Filewalker manifest and generates GitHub raw URLs. The name is
precise: a fletcher makes arrows; this tool makes pointers.

The pylint complaints were: four trailing spaces and one missing docstring.

Both models saw 607 lines, found the git branch detection section
(~80 lines), and discarded the other 525 lines entirely.

- e2b: extracted function names, generated mock stubs, then looped.
- e4b: read the git detection code, understood it, rewrote it in its
  own style using os.popen instead of subprocess, built a new main()
  around only that functionality. Confident. Wrong. Unusable.

Neither model hallucinated from nothing. They both performed feature
extraction — identified the most recognizable pattern and amplified it,
discarding everything that made the tool what it actually is.

The models didn't fail because fletcher is complicated. Fletcher is the
simplest package in the repo. They failed because git detection is
semantically loud to a model trained on millions of git utilities.
The string concatenation that is the actual point of the tool was
invisible to them.

**Finding:** Feature extraction blindness is triggered by semantic
weight, not code complexity. A simple tool with one high-signal section
is more dangerous than a complex tool with uniform signal distribution.

### Context window truncation as a cause

The Ollama server log showed at least two calls with:
`truncating input prompt: limit=4096 prompt=4331`

Fletcher at ~2000 tokens of source + pylint report + prompt was
exceeding the context window on e2b. The model wasn't just choosing
to ignore the string concatenation — in some cases it literally
couldn't see it.

### Incomplete code behavior

mcpkit is an unfinished package. Both models behaved very differently
on incomplete vs finished code:
- Finished code (setupkit, viewkit): mostly correct output
- Incomplete code (mcpkit): confident hallucination (e4b) or loops (e2b)

**Finding:** These models are only safe for automated cleanup on
finished, stable code. Running on work-in-progress is actively
dangerous — the model will complete your unfinished work with whatever
it imagines belongs there.

### Pylint report size as predictor

Strong correlation between pylint report size and failure mode:
- Small reports (353 chars): mostly correct output
- Large reports (6000+ chars): increased hallucination
- Very large reports (16000-36000 chars): near-certain hallucination

test_view_builder.py had a 36,654 char pylint report. Both models
hallucinated completely.

### The wall clock time finding

The baseline 02 run processed 81 files × 2 models with file-outer,
model-inner loop order. Every file required two model load/evict cycles.
Ollama logs showed each swap taking 3-5 seconds of pure overhead.
Total run time: [INSERT FROM session_log_baseline_02_2026-05-16.txt]

After restructuring to model-outer, file-inner loop order:
Total run time: [INSERT FROM 02a session log]

Each model loads once, processes all 81 files, then unloads.
The improvement is a practical finding for anyone running local LLM
experiments on consumer hardware with limited VRAM.

**Finding:** Iteration order matters for local inference in ways it
doesn't for API calls. Naive loop structure can add significant
overhead that has nothing to do with the actual task.

### Reproducibility

TODO: Run 02a twice without changing anything. Compare output pairs.
At temperature 0.1, output should be highly deterministic — but does
e4b hallucinate fletcher the same way twice? Does e2b loop on the same
files? Variance across runs at low temperature is an underexplored
angle in local LLM evaluation writing.

### The Gemini consultation

Used Gemini to analyze the findings mid-experiment. Gemini's analysis
of why e4b hallucinates vs e2b's failure modes was accurate and useful.
Key term from Gemini: "Feature Extraction Blindness."

The consultation itself is worth documenting — using one AI to help
evaluate another is a legitimate multi-LLM workflow finding. Claude
handled mechanical code work; Gemini provided analysis of model behavior.
Each had a distinct role.

### The results web UI

TODO: Build a single static HTML file that reads run_log.yaml and
result text files, renders results in the browser. Side-by-side
e2b vs e4b comparison per file, filterable by status (complete /
bailed / looped / truncated). No server needed — fits offline-first
philosophy. Potentially generated by Gemma as a demonstration.

---

## Tools Generated as Side Effects

1. **treekit** — reads markdown spec, scaffolds directory structure
2. **skeleton_reader** → eventually **skeletonkit** in dev-utils
3. **repo_reader** → eventually part of **filekit** in dev-utils
4. **venvkit** — config-driven venv management (noted, not built yet)

Three tools minimum, possibly four, emerged from building a framework
to test a model. Include this count explicitly — it's a good exhibit
for the thesis.

---

## Voice Experiments — The Strong Hook

"Write this README in the voice of Lord Vetinari" is immediately legible
to a dev.to audience as both technically interesting and deeply silly
in the right way.

The axis table (from ChatGPT conversation, genuinely useful):

| Voice | Scale | Sentence Shape | Humor Source | Emotional Temperature |
|---|---|---|---|---|
| Hitchhiker's | Cosmic | Long, digressive | Absurdity | Warm-chaotic |
| Vetinari | Civic/Human | Precise, controlled | Understatement | Cold-controlled |
| Terminator | Tactical | Minimal | None/intimidation | Machine-flat |

The "forbidden rhetorical moves" framing stabilizes stylistic separation
in smaller models. Worth explaining.

Kafka/Gregor Samsa voice: first-person interiority, bureaucratic dread
made personal. The results have been noted. The door may or may not
be locked.

---

## The Caulfield Principle (mention this)

Write down how you actually do the thing, not how you think you should
do it. Applied throughout prompt writing. The mechanical prompts
benefited most. Worth mentioning in the article.

---

## Future Posts (Series)

Each dev-utils tool has a natural origin story. Post one per week.
See if anyone reads.

- **config_rules.md post** — broke my own rules, Ollama didn't connect,
  spent a morning formalizing rules instead of a five-minute fix.
  Hook: "I broke my own rules and here's what it cost me."

- **treekit post** — leaning into what you know about yourself.
  Recognized a failure mode before it happened and built around it.
  Can discuss without explicitly mentioning ADHD — universally relatable
  to anyone who knows their own failure modes.

- **One post per dev-utils tool** — fletcher, imagekit, setupkit, etc.
  Each has an origin story.

- **Reproducibility in local LLM evaluation** — does e4b hallucinate
  fletcher the same way twice? Variance at low temperature is an
  underexplored topic.

- **Local LLM hardware constraints** — iteration order, VRAM pressure,
  model swap overhead. Practical findings for consumer hardware.

---

## Curator / Postgres Integration

The postgres projects tracking database already exists with Curator
CRUD webui. Plan:
- Create "code templates" project in Curator, link template files to it
- Integrate experiment definitions into the projects tracking DB
- Experiment registry becomes queryable, filterable, auditable
- Not just YAML files — proper infrastructure for systematic LLM evaluation
- Eventually: task-to-commit-message pipeline via "the Bosun" git tool

---

## The Bosun (mention briefly)

Future dev-utils tool: git ceremony automation. Task description →
commit message → stage → commit → push, driven from the project
database. The Curator already has the task data. The Bosun acts on it.
"I forgot to add the commit message again" is a universal developer
experience. The tools we build are almost always responses to our own
specific failure modes.

---

## Unseen University (future, not for this post)

Separate experiment series. The faculty alone would generate fascinating
output. Ponder Stibbons explaining entropy would be a legitimate
scientific document. Not today.

---

## macOS Tahoe Update Chaos (sidebar or footnote)

A forced macOS Tahoe update at the start of a session caused:
display profile reset, browser extensions wiped, Docker Desktop dead,
Podman DNS errors. The fix was podman-compose (Homebrew) not
podman compose (built-in). Four acts: the update, the blue screen,
the missing extension, the Apple password hostage situation.
Write as a separate whycantyoujust.tech blog post when the contest
is done.

Looking at those notes made me realize that the smaller model saying
"I can't do that" instead of making shit up the way the larger model
did makes me think I'd rather use the "less capable" model because I
might not have to fact check it as often.

Calibrated refusal is a feature, not a limitation. e2b said "I have
no filesystem context" — accurate and useful. e4b invented a complete
fictional repo with confidence — wrong, requires fact-checking.

For production use where correctness matters more than fluency, a model
that knows what it doesn't know is more valuable than one that fills
gaps with plausible-sounding fiction. It also has implications for
prompt design: if e2b refuses cleanly when context is missing, it's
a reliable signal that context injection is working correctly.
